<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <TextView
        android:id="@+id/place_explore"
        android:layout_marginLeft="10dp"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toTopOf="@+id/place_tv"
        android:text="Explore"
        android:textSize="16sp"
        android:textColor="#4457D1"
        />
    <TextView
        android:id="@+id/place_tv"
        android:layout_marginLeft="10dp"

        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/place_explore"
        app:layout_constraintBottom_toBottomOf="parent"
        android:text="Pyramids of Giza"
        android:textSize="20sp"
        android:textStyle="bold"
        android:textColor="#4457D1"
        android:layout_marginBottom="20dp"
        />
    <ImageView
        android:id="@+id/place_iv"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:layout_constraintTop_toTopOf="@+id/place_explore"
        app:layout_constraintBottom_toBottomOf="@+id/place_tv"
        app:layout_constraintRight_toRightOf="parent"
        android:layout_marginRight="10dp"
        android:src="@drawable/ic_launcher_background"
        android:maxWidth="200dp"
        android:maxHeight="200dp"
        android:minWidth="100dp"
        android:minHeight="100dp"
        android:adjustViewBounds="true"
        />

</androidx.constraintlayout.widget.ConstraintLayout>